<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moa.mybatis.mobile.HostRequestMapper">

    <select id="HostRequestList" parameterType="int" resultType="com.moa.mobile.model.vo.RequestListInfo">
        select nickname "nick",TRANSACTION_PRICE "price",
        APPLICATION_DATE AS "date",To_char(APPLICATION_DATE,'hh24:mi:ss') "time",TRANSACTION_STATE
        from store_request_view
        where transaction_state = '승인대기중' AND host_id = 7
        order by APPLICATION_DATE asc
    </select>

    <select id="selectTransaction" resultType="String">
        SELECT TRANSACTION_WAY FROM SR_TRANSACTION_TYPE_TB
    </select>

</mapper>